publishing {
    publications {
    	mavenJava(MavenPublication) {
    		artifactId "${parent.name}-${project.name}"
    		artifact sourceJar
    		artifact javadocJar
    		from components.java
    	}
    }
}
